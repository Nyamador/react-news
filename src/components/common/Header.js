import React from 'react';
import './header.css';

export default function Header(){
    return (
        <header className={"header flex-row al-ctr"}>
            <div className={"header-wrapper"}>
            <p>Header for news</p>
            </div>
        </header>
    );
}